#include <stdio.h>

int main() {
  // Declara una cadena de caracteres con el texto "¡HolaMundo!"
  char hola[] = "¡HolaMundo!";
  // Imprime la cadena almacenada en 'hola' en la pantalla
  printf("%s\n", hola);
  return 0; // Indica que el programa terminó correctamente
}